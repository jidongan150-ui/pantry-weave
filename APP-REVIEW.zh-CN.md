# 应用复查 · 2026-09-19（北京时间）

## 识别路线调整 · 2026-09-20（北京时间）

暂停继续增加 OCR 特例，新增“导入 ChatGPT 识别结果”。复制说明后在 ChatGPT 中识别图片，将完整回复粘回应用，即可预览并一次加入清单。它能减少逐项抄写，但仍需要切换页面，网页没有自动调用 ChatGPT。

本轮直接查看三张旧图和两张新公开食谱页，记录当前助手辅助读取与 OCR 的差异，详见 VISION-COMPARISON.zh-CN.md。辅助读取受到可见上下文影响，不是盲测；没有 API、小模型成绩或真实费用数据，不能宣称已解决自动识别。

## 最新进展 · 2026-09-20（北京时间）

本轮优先修复识别可靠性：自动定位食材栏并放大复读，减少双语分栏混入做法、左右食材连成一项的问题。不确定分数和互相冲突的用量保留为待核对，不猜数。多次读取只是减少错误的措施，不能保证一致的读数就正确。

10 张完整图片回测中，蔬菜汤、番茄吞拿鱼杯和西兰花浓汤的食材覆盖有改善；困难汤谱仍有漏项，图片叠字仍有失败，部分用量因谨慎处理反而需要更多核对。新增三页来自同一本已下载食谱册，不能当作独立随机准确率。详见 OCR-CHECKS.md。

54 项核心测试、类型检查、生产构建，以及中英文购物流程、320 像素布局、图片下载、拖放、框选失败恢复和取消识别检查通过。没有启用 API 或新增依赖。下一阶段真正需要验证的是不同手机拍照和首次使用者能否少修改完成购物；目前不应声称达到客观 80 分。

结论：目前最影响使用的是识别可靠性和流程分散。此次检查未发现可以绕过账户权限的漏洞，但不能据此宣称应用绝对安全。

## 已修复

| 问题 | 影响 | 修复 |
| --- | --- | --- |
| 未知用量被写成“适量” | 可能把漏识别的主食材数量当作自由添加 | 总清单和文字下载明确显示“用量未注明”；显式“适量”保留 |
| 范围、相加和小数逗号被当作确定数量 | 例如把 `2 to 3` 当作 2 | 保留原文供核对，不填写确定数量 |
| 识别时拖入多个文件，旧任务继续完成 | 报错后仍可能出现旧图结果 | 无效替换会取消识别并清空旧预览 |
| 翻译不可用但仍让用户重复尝试 | 看起来像按钮无效 | 明确说明不可用，保留原文；取消时释放翻译器 |
| 未接通邮件却显示“发送购物清单” | 引导用户走进无法完成的流程 | 显示实际可用的“导出购物清单” |

## 安全与验证

- 31 项自动化测试通过，覆盖输入、计算、令牌、权限、限流、识别与翻译回退。
- 在真实 Convex 开发环境使用独立测试身份验证保存、恢复、实时同步，以及拒绝匿名和其他账户访问；测试食谱已移除。
- 生产依赖审计未发现已知漏洞。检查了账户归属、公开/内部函数边界、文本渲染、私钥位置和文件类型限制。
- 浏览器检查覆盖 PNG 下载、原文保留、换图取消、未知数量的文字导出、翻译不可用提示及 320px 布局。
- 本次真实云端测试使用测试令牌；未重新验证线上完整的交互式登录过程。网页抓取、模型 API 和邮件服务仍未启用，不能宣称它们已通过联测。
- 网站仍仅所有者可访问。退出登录后的短期令牌最多仍有效五分钟；公开上线前需保留限流并验证完整登录流程。不要把依赖 Sites 身份请求头的 Worker 单独公开。

## 更简单、更省力的路线

1. **已统一最终保存入口。** 主清单新增“保存购物清单图片”：合并、换算并排除勾选项后生成单张 PNG，支持中英文切换。扫描卡仍用于核对原始食谱，购物时可保存主页面的最终清单。
2. **已整理主流程：上传 → 核对修改 → 加入清单 → 保存。** 上传成为首页主要入口；未连接的链接导入折叠，未连接的邮件不显示按钮。核对先显示编辑表格，单道菜图片预览可按需展开。新增原图对照、缺失项提示、框选重识别、失败后手填，以及加入后的食谱编辑。
3. **不要无限增加识别规则和翻译词表。** 五张真实图片已经显示，多栏、艺术字和手写会带来持续维护成本。Tesseract 官方也记录了分栏/表格与输入质量限制：[识别质量指南](https://github.com/tesseract-ocr/tessdoc/blob/main/ImproveQuality.md)。短期把它明确定位成需要核对的辅助导入，保留手动更正即可。
4. **需要明显提升自动化时，再接一个可用的视觉模型服务。** 让同一次处理返回食材、数量、单位、原文依据和中英文名称，统一校验、统一核对。这样预计比维护多个特殊规则省力，但会增加费用、外部数据处理和服务依赖；准确率仍需用同一批真实图片验证，不保证全部正确。没有 API 时无需阻塞当前应用，也不建议为了这一步重写整个项目。
5. **浏览器完整翻译只能做可选增强。** 它受设备、浏览器和语言包支持限制，不能作为保证所有用户都能使用的主路径：[Chrome 官方说明](https://developer.chrome.com/docs/ai/get-started)。当前词表翻译可用，未知文字会保留原文。
6. **已统一本地语言显示。** 扫描卡、保存后的菜谱、购物清单、文字和 PNG 复用同一显示翻译入口，切换语言同步主页面。未知文字仍保留原文并提示；可选的浏览器完整翻译只增强扫描预览，不会改写源数据。

最省维护成本的选择是继续使用现有 Convex 和登录系统，先做好最终清单导出与核对体验；暂不增加邮件、多个抓取服务或复杂的新架构。

## 本轮验收与仍缺的证据

29 项核心测试及 9 项后端测试通过。真实云端确认编辑保存、重新读取、实时同步及拒绝旧版本／其他账户修改。浏览器验证了编辑取消、原文保留、未知数量、裁剪和手填恢复、中英文与 320px 布局。新增三张真实图片依然暴露遗漏和失败，详见 `OCR-CHECKS.md`。这些改进减少了纠错成本，不能据此宣称识别准确率已达 80% 或产品已获独立的 80 分评价。

尚未邀请首次使用的人完成任务；需要观察他们是否能不经指导完成两道菜的导入、修正、人数换算和清单保存。保持私有访问，待用户决定后再安排外部试用。

## 2026-09-19：按日常买菜流程简化

根据用户再次试用的反馈，把默认扫描结果从逐项大表格改为简短食材列表，点击某一项才展开编辑；原图、原文和框选重试放到最后，默认折叠。上传按钮直接打开图片选择器，另设拍照入口。手机结果页保留可见的“加入购物清单”按钮。

修正此前仍然存在的语言缺口：扫描结果的食材名称和展开编辑框现在都显示当前语言，切换或聚焦不会改写源数据。自由输入仍可能缺少可靠译文，不能宣称完整翻译。购物时去掉重复的做法原文，只保留用量和可选／替代提示；原文仍保存在食谱编辑处。

原食谱人数可留空：保留原方用量，不强迫用户猜测人数；与按人数换算的条目分开标记。以后填写原人数即可换算。图片没写的数量仍显示未知，不以虚构数字制造“一键完成”的假象。

按家庭采购场景做了自动化模拟：上传真实番茄炒蛋图、不填写任何字段、切换中英文、加入清单、勾掉已有食材、保存图片、再加入第二道菜。34 项核心测试、10 项后端测试、类型检查和构建通过；真实开发环境验证了空人数保存、重新读取、实时同步及权限隔离。本地正式构建验证了 320px 布局、拖放、框选及手填恢复，无浏览器页面错误。

仍然不能把复杂图片处理包装成完全自动化：三张回归图片中，两张仍有遗漏，一张仍失败，详见 OCR-CHECKS.md。当前改动减少操作；要显著减少漏项和自由文本翻译缺口，后续需评估可用的视觉模型，并用未参与调试的新图片独立验收。此轮没有新增外部服务或依赖，也没有进行真人首次试用。

## 2026-09-19：采购清单的三项改进

1. **中英文统一合并。** 常见食材使用显示翻译的同一词表，例如 onions／洋葱、milk／牛奶，补齐 pieces／个等单位。不同食物和带处理说明的名称不做模糊合并。原本“其他”分类中的已知食材自动进入相应采购分类。
2. **可选材料按需购买。** 扫描中明确识别的可选项默认跳过，明确的替代组二选一或不选；未选材料保存在食谱中，可以再次编辑选择。扫描预览、主清单、文字和 PNG 导出排除未选项。旧扫描中的自动可选备注在编辑草稿中转为可选项，保存前不修改云端记录。
3. **保留未变化的勾选。** 添加、修改、删除食谱或修改人数时，按合并后的实际需求比较，保留未变化项目，数量、备注等变化的项目重新确认。旧版中英文分开的项目首次合并时，避免继承其中一个旧项目的局部勾选，安全地要求重新确认一次。

39 项核心测试及 12 项后端测试通过，覆盖等价名称／单位、不同食物分离、可选项保存与排除、同组双选拒绝、旧勾选兼容、权限隔离及旧版本编辑拒绝。真实开发环境和本地正式构建的浏览器流程分别检查云端状态和采购体验。实际番茄炒蛋图及手动中英文食谱用于回归；此轮没有进行新的独立识别准确率测试，也没有启用模型 API。手机窄屏已检查，物理相机、弱网和首次真人试用仍待验证。

## 2026-09-20：份数提示、食材分组与独立样本检查

清单和食谱区顶部区分全部换算、部分换算、全部保留原方用量，不再把人数未知的食谱标成已换算。同一食材在页面、文字和图片中集中展示；不同单位、未知用量、未换算用量仍保留各自明细、来源和独立勾选，避免误算总数。种类统计按食材计数，待购项和进度仍按可勾选的需求计数。

44 项核心测试和 12 项后端测试、类型检查、正式构建通过。浏览器验证混合份数、全部未知份数、局部勾选、修改后重新计算、中英文 320/390/1440px 布局和 PNG 下载；已加载页面离线后仍可导出图片。原采购流程回归通过。此处的手机测试为浏览器模拟，并非实体手机拍摄或真人首次使用。

三份新的公开食谱页面均未达到无需修改的实用标准，详见 OCR-CHECKS.md。针对发现的严重数量错误加上保守检查：疑似分数残片、过大的杯／勺用量提示核对而不猜测正确值；英文人数范围不再取下限。复杂中英双栏仍可能漏项、把做法当成食材。没有启用模型 API，不宣称识别准确率达标。撤销删除、完整自由文本翻译、物理手机和首次真人验收尚未实现／完成。
