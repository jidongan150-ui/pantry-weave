# 图片识别接入与费用决策

核实日期：2026-09-21。Puter 账户创建失败，主流程调整为 ChatGPT 识图后点击链接导入。

## 当前选择：ChatGPT 识图，链接回到清单

在已有 ChatGPT 对话中复制一次本站提供的说明并上传图片；ChatGPT 返回食谱链接，打开即进入食材核对。后续在同一对话继续发图。网站本身不调用模型，也不会访问聊天、复用登录 cookie 或消耗共享 API 密钥。受用户 ChatGPT 功能和额度限制，不承诺无限免费。

JSON 放在 URL fragment（#recipe=）中，不随页面 HTTP 请求发送；网站严格限制长度和字段，读到后清除地址栏中的内容，用户确认前不保存。链接本身包含食谱，分享链接即分享食谱。模型生成长链接可能出错，仍提供完整 JSON 粘贴恢复入口。这是普通链接交接流程，不是已发布的 ChatGPT 插件或 Apps SDK 集成。

已验证当前助手读图整理的数据通过链接进入本地生产预览、核对、合并与 PNG 导出。不是独立盲测，也没有验证所有用户在 ChatGPT 中生成的链接都有效。本站不再把 Puter 登录当主流程。

## 之前试验的 Puter 路线（保留可选）

使用开源 Puter.js 2.6.3，由每位用户登录自己的 Puter 账户，通过它调用
Gemini 2.5 Flash-Lite。无需开发平台账户或 API 密钥。Convex 仍负责购物清单
数据和同步；没有把现有应用迁移到 Puter。

依据：https://docs.puter.com/user-pays-model/ 。Puter 提供每用户月度免费额度，
具体以账户为准；额度用完可能弹出付费升级。不能宣传无限免费。AI 额度由
使用者承担，现有 Convex/网站托管费用依然由项目承担。

本机已读取官方公开模型目录
https://api.puter.com/puterai/chat/models/details ，其中 gemini-2.5-flash-lite
的 provider 为 gemini，支持 image 输入。浏览器 SDK 成功加载并打开
puter.com/action/sign-in 弹窗。后续等待页面渲染的检查确认可显示
Create Free Account 表单及 Log In 入口；没有代用户注册或登录。
模型响应速度、实际额度消耗、识别准确率及线上授权均未验证。

## 调用约束

先显示图片与发送对象，点击 AI 按钮后才授权并发送。图片先重编码为 JPEG，
最多 2400 边长、400 万像素，去掉原文件元数据与文件名。
每次识别只调用一次固定模型，上限 5000 输出 tokens，90 秒等待，不自动切换
更贵模型、不由应用重试推理。SDK 可能在未授权错误时刷新身份并重放请求。
取消不采用迟到结果，但无法撤销已发请求或承诺返还额度。
结果通过现有严格 schema 才进入核对页，不自动写库。选择加入后走原有登录
用户保存路径。没有共享开发者密钥，没有新增公开推理端点。
新页面会清除本站保存的 Puter 令牌，首次使用重新确认 AI 账户，避免在本应用
退出／换用户后静默沿用上一位使用者的 AI 额度。授权由 Puter SDK 管理。

本地识别仍可选，图片不上传；免费 ChatGPT 手动识别后粘贴结果仍是备用。
ChatGPT 登录只证明本应用用户身份，不提供调用模型的 API 权限。

## 其他路线与成本

- Gemini API 有免费层，需要开发账户和密钥，限额按账户/项目为准。
  https://ai.google.dev/gemini-api/docs/pricing
  https://ai.google.dev/gemini-api/docs/rate-limits
  免费层与付费层的数据使用政策不同，不能忽略。
- Gemini 2.5 Flash-Lite 页面列出的标准付费价格为每百万输入 tokens 0.10 美元、
  输出 0.40 美元。假设每张图共 3000 输入 + 1000 输出 tokens，100 人每人
  10 张约 0.70 美元推理费。这是算例，不是实测账单；不含托管、税费、重试。
  若未来采用项目付费，应先实施服务端每日总预算、用户限额和并发限制。
- Cloudflare Workers AI 免费层每天 10000 Neurons，**不是 10000 张图片**；
  超出免费层需要付费计划。https://developers.cloudflare.com/workers-ai/platform/pricing/
- OpenRouter 免费模型通常限额较低且可用性会变，不能承诺用于不限量公开服务。
  https://openrouter.ai/docs/faq
- 免费 ChatGPT 可以由用户在 ChatGPT 内手动使用其功能；没有把网页登录变成
  本站自动免费推理接口的已验证接入方式。不采集会话 cookie、不自动操作网页
  冒充 API、不循环注册试用账号。

## 验证与提交前剩余事项

新增 adapter 测试覆盖模型/输出上限、未登录不请求、格式错误不落库、额度错误
不重试、取消后忽略结果。浏览器使用实际 SDK 检查加载、弹窗打开与关闭；
使用替身响应检查食材核对、额度错误和 320px 无横向溢出。这些不是准确率测试。

实际账户可用后，对原番茄炒蛋图、两张英文公开食谱和至少一张新图片记录模型
名称、耗时、缺漏食材、错误数量、额度消耗。未通过前不得将其宣称为可靠自动
识别。网站已于 2026-09-21 获用户授权公开并通过无登录访问检查。
公开 GitHub 仓库、最终演示视频、社交发布和正式提交仍需完成。
